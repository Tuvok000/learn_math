#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void rand_gen(int *a, int *b, int *c, int *d, int min, int max)
{
  //middle of range: 
  *a = (rand() % 20)

int main()
{
  time_t t;
  int i, a, b, c, d;
  
  FILE *output, *answers;
  output = fopen("output.txt", "w");
  answers = fopen("answers.txt", "w");
  //fprintf(output, "%d%c", var)

  //initialize random number generator
  srand((unsigned) time(&t));

  for(i = 0; i < 20; i++)
    {
      fprintf(output, "
